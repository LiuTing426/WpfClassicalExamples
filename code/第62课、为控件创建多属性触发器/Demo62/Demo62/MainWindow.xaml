<Window x:Class="Demo62.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Demo62"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <MultiTrigger>
                    <!--条件-->
                    <MultiTrigger.Conditions>
                        <Condition Property="IsEnabled" Value="True"/>
                        <Condition Property="IsKeyboardFocused" Value="True"/>
                    </MultiTrigger.Conditions>
                    <!--所有条件满足之后 Setters才会起作用-->
                    <MultiTrigger.Setters>
                        <Setter Property="Foreground" Value="Green"/>
                        <Setter Property="Background" Value="LightGreen"/>
                    </MultiTrigger.Setters>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <StackPanel>
        <TextBox Text="聚焦在这里" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        <TextBox Text="聚焦在这里" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" IsEnabled="False"/>
    </StackPanel>
</Window>
